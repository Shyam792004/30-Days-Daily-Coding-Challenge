Binary Search
Solutions:
int search(vector<int> &nums, int target) {
    // Write your code here.
    int n=nums.size();
    int flag=0;
    int c=0;
    for(int i=0;i<n;i++)
    {
        if(target==nums[i])
        {
            c=i;
           flag=1;

           break;
        }
    }
    if(flag==0)
    {
        return -1;
    }
    else{

        return c;
    }
}
