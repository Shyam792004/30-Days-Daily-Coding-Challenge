Largest element in the array

CODING:
#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    // Write your code here.
    int a=arr[0];
    for(int i=0;i<n;i++)
    {
        if(a<arr[i])
        {
            a=arr[i];
        }
    }
    return a;
}
